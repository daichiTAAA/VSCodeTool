# Development token (EnvTokenProvider)
# Copy to .env and set your PAT when DI_PROVIDER=env
DATABRICKS_PAT=replace-with-your-databricks-personal-access-token

# Key Vault settings (KeyVaultTokenProvider)
# Used when DI_PROVIDER=keyvault
KEYVAULT_URL=https://<your-key-vault-name>.vault.azure.net/
SECRET_NAME=databricks-pat

# Optional
DATABRICKS_PAT_ENV_KEY=DATABRICKS_PAT
DI_PROVIDER=env

# Databricks Workspace (required)
DATABRICKS_WORKSPACE_URL=https://adb-xxxxx.azuredatabricks.net
