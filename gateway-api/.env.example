#############################################
# Databricks Gateway API - .env (example)
# Copy to gateway-api/.env and edit values
#############################################

# --- Provider mode (token source) ---
# 'env'     : Use PAT from this file / container env
# 'keyvault': Use Azure Key Vault (Managed Identity) to fetch PAT
DI_PROVIDER=env

# --- EnvTokenProvider settings ---
# Personal Access Token (used when DI_PROVIDER=env)
DATABRICKS_PAT=replace-with-your-databricks-personal-access-token

# --- KeyVaultTokenProvider settings ---
# Used when DI_PROVIDER=keyvault
KEYVAULT_URL=https://<your-key-vault-name>.vault.azure.net/
SECRET_NAME=databricks-pat

# --- Databricks Workspace (required) ---
DATABRICKS_WORKSPACE_URL=https://adb-xxxxx.azuredatabricks.net

# --- Request logging (app layer) ---
# REQUEST_LOGGING: off | basic | body
REQUEST_LOGGING=basic
# Maximum bytes of body to log when REQUEST_LOGGING=body
MAX_BODY_LOG=4096

# --- Uvicorn server logging ---
# These args are appended to the uvicorn command by docker-compose
# Examples:
#   UVICORN_CMD_ARGS="--access-log --log-level info"
#   UVICORN_CMD_ARGS="--access-log --log-level debug"
UVICORN_CMD_ARGS=--access-log --log-level info
